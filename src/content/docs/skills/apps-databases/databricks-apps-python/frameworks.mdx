---
title: Supported Frameworks
description: All frameworks below are pre-installed in the Databricks Apps runtime.
sidebar:
  order: 3
---









`dash-bootstrap-components`

```python
import dash
import dash_bootstrap_components as dbc

app = dash.Dash(
    __name__,
    external_stylesheets=[dbc.themes.BOOTSTRAP, dbc.icons.FONT_AWESOME],
    title="My Dashboard",
)
```




`["python", "app.py"]`
`app.run(port=int(os.environ.get("DATABRICKS_APP_PORT", 8000)))`
`request.headers.get('x-forwarded-access-token')`


`dbc.themes.BOOTSTRAP``dbc.icons.FONT_AWESOME`
`"success"``"danger"``dbc.Badge`
`prevent_initial_call=True`
`dcc.Store`









`@st.cache_resource`

```python
import streamlit as st
from databricks.sdk.core import Config
from databricks import sql

st.set_page_config(page_title="My App", layout="wide")  # Must be first!

@st.cache_resource(ttl=300)
def get_connection():
    cfg = Config()
    return sql.connect(
        server_hostname=cfg.host,
        http_path="/sql/1.0/warehouses/<id>",
        credentials_provider=lambda: cfg.authenticate,
    )
```




`["streamlit", "run", "app.py"]`
`st.context.headers.get('x-forwarded-access-token')`


`st.set_page_config()`
`@st.cache_resource``@st.cache_data(ttl=...)`
`st.form()`
`st.column_config`









`gr.Request`

```python
import os
import gradio as gr
import requests
from databricks.sdk.core import Config

cfg = Config()

def predict(message, request: gr.Request):
    user_token = request.headers.get("x-forwarded-access-token")
    # Query model serving endpoint
    headers = {**cfg.authenticate(), "Content-Type": "application/json"}
    resp = requests.post(
        f"https://{cfg.host}/serving-endpoints/my-model/invocations",
        headers=headers,
        json={"inputs": [{"prompt": message}]},
    )
    return resp.json()["predictions"][0]

demo = gr.Interface(fn=predict, inputs="text", outputs="text")
port = int(os.environ.get("DATABRICKS_APP_PORT", 8000))
demo.launch(server_name="0.0.0.0", server_port=port)
```




`["python", "app.py"]`
`server_port=int(os.environ.get("DATABRICKS_APP_PORT", 8000))`
`request.headers.get('x-forwarded-access-token')``gr.Request`



`gr.ChatInterface`
`gr.Blocks`











```python
from flask import Flask, request, jsonify
from databricks.sdk.core import Config
from databricks import sql

app = Flask(__name__)
cfg = Config()

@app.route("/api/data")
def get_data():
    conn = sql.connect(
        server_hostname=cfg.host,
        http_path="/sql/1.0/warehouses/<id>",
        credentials_provider=lambda: cfg.authenticate,
    )
    with conn.cursor() as cursor:
        cursor.execute("SELECT * FROM catalog.schema.table LIMIT 10")
        return jsonify(cursor.fetchall())
```




`["gunicorn", "app:app", "-w", "4", "-b", "0.0.0.0:8000"]`
`request.headers.get('x-forwarded-access-token')`



`-w 4`
`request.headers`









```python
from fastapi import FastAPI, Request
from databricks.sdk.core import Config
from databricks import sql

app = FastAPI(title="My API")
cfg = Config()

@app.get("/api/data")
async def get_data(request: Request):
    user_token = request.headers.get("x-forwarded-access-token")
    conn = sql.connect(
        server_hostname=cfg.host,
        http_path="/sql/1.0/warehouses/<id>",
        access_token=user_token,
    )
    with conn.cursor() as cursor:
        cursor.execute("SELECT * FROM catalog.schema.table LIMIT 10")
        return cursor.fetchall()
```




`["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]`
`request.headers.get('x-forwarded-access-token')``Request`


`/docs``/redoc`
`asyncio.to_thread()`










```python
import reflex as rx
from databricks.sdk.core import Config

cfg = Config()

class State(rx.State):
    data: list[dict] = []

    def load_data(self):
        from databricks import sql
        conn = sql.connect(
            server_hostname=cfg.host,
            http_path="/sql/1.0/warehouses/<id>",
            credentials_provider=lambda: cfg.authenticate,
        )
        with conn.cursor() as cursor:
            cursor.execute("SELECT * FROM catalog.schema.table LIMIT 10")
            self.data = [dict(zip([d[0] for d in cursor.description], row)) for row in cursor.fetchall()]
```



`["reflex", "run", "--env", "prod"]`
`session.http_conn.headers.get('x-forwarded-access-token')`







`requirements.txt`
`requirements.txt`
`Config()`
`DATABRICKS_APP_PORT``app.yaml`


