---
title: Geospatial SQL and Collations in Databricks SQL
description: "Databricks SQL provides comprehensive geospatial support through two function families: H3 functions for hexagonal grid indexing and ST functions for standar..."
sidebar:
  order: 99
---











`GEOMETRY`
`GEOGRAPHY`


`GEOMETRY`
`GEOGRAPHY`



`GEOMETRY``GEOGRAPHY`





`ST_GeomFromWKT``ST_GeogFromWKT``ST_AsWKT``ST_AsText`
`ST_GeomFromWKB``ST_GeogFromWKB``ST_AsWKB``ST_AsBinary`
`ST_GeomFromEWKT``ST_GeogFromEWKT``ST_AsEWKT`
`ST_GeomFromEWKB``ST_AsEWKB`
`ST_GeomFromGeoJSON``ST_GeogFromGeoJSON``ST_AsGeoJSON`
`ST_GeomFromGeoHash``ST_PointFromGeoHash``ST_GeoHash`











`h3_longlatash3(lon, lat, resolution)``BIGINT`
`h3_longlatash3string(lon, lat, resolution)``STRING`
`h3_pointash3(geogExpr, resolution)``BIGINT`
`h3_pointash3string(geogExpr, resolution)``STRING`
`h3_polyfillash3(geogExpr, resolution)``ARRAY<BIGINT>`
`h3_polyfillash3string(geogExpr, resolution)``ARRAY<STRING>`
`h3_coverash3(geogExpr, resolution)``ARRAY<BIGINT>`
`h3_coverash3string(geogExpr, resolution)``ARRAY<STRING>`
`h3_tessellateaswkb(geogExpr, resolution)``ARRAY<STRUCT>`
`h3_try_polyfillash3(geogExpr, resolution)``ARRAY<BIGINT>`
`h3_try_polyfillash3string(geogExpr, resolution)``ARRAY<STRING>`
`h3_try_coverash3(geogExpr, resolution)``ARRAY<BIGINT>`
`h3_try_coverash3string(geogExpr, resolution)``ARRAY<STRING>`
`h3_try_tessellateaswkb(geogExpr, resolution)``ARRAY<STRUCT>`





`h3_boundaryaswkt(h3CellId)``STRING`
`h3_boundaryaswkb(h3CellId)``BINARY`
`h3_boundaryasgeojson(h3CellId)``STRING`
`h3_centeraswkt(h3CellId)``STRING`
`h3_centeraswkb(h3CellId)``BINARY`
`h3_centerasgeojson(h3CellId)``STRING`





`h3_h3tostring(h3CellId)`
`h3_stringtoh3(h3CellIdString)`





`h3_resolution(h3CellId)`
`h3_toparent(h3CellId, resolution)`
`h3_tochildren(h3CellId, resolution)`
`h3_maxchild(h3CellId, resolution)`
`h3_minchild(h3CellId, resolution)`
`h3_ischildof(h3CellId1, h3CellId2)`





`h3_distance(h3CellId1, h3CellId2)`
`h3_try_distance(h3CellId1, h3CellId2)`
`h3_kring(h3CellId, k)`
`h3_kringdistances(h3CellId, k)`
`h3_hexring(h3CellId, k)`





`h3_compact(h3CellIds)`
`h3_uncompact(h3CellIds, resolution)`





`h3_isvalid(expr)`
`h3_validate(h3CellId)`
`h3_try_validate(h3CellId)`
`h3_ispentagon(h3CellId)`



```sql
-- Convert coordinates to H3 cell at resolution 9
SELECT h3_longlatash3(-73.985428, 40.748817, 9) AS h3_cell;

-- Index taxi trips by pickup location
CREATE TABLE trips_h3 AS
SELECT
  h3_longlatash3(pickup_longitude, pickup_latitude, 12) AS pickup_cell,
  h3_longlatash3(dropoff_longitude, dropoff_latitude, 12) AS dropoff_cell,
  *
FROM taxi_trips;

-- Fill zip code polygons with H3 cells for spatial indexing
CREATE TABLE zipcode_h3 AS
SELECT
  explode(h3_polyfillash3(geom_wkt, 12)) AS cell,
  zipcode, city, state
FROM zipcodes;

-- Find all trips picked up in a specific zip code using H3 join
SELECT t.*
FROM trips_h3 t
INNER JOIN zipcode_h3 z ON t.pickup_cell = z.cell
WHERE z.zipcode = '10001';

-- Proximity search: find all H3 cells within 2 rings of a location
SELECT explode(h3_kring(h3_longlatash3(-73.985, 40.748, 9), 2)) AS nearby_cell;

-- Aggregate trip counts and get centroids for visualization
SELECT
  dropoff_cell,
  h3_centerasgeojson(dropoff_cell):coordinates[0] AS lon,
  h3_centerasgeojson(dropoff_cell):coordinates[1] AS lat,
  count(*) AS trip_count
FROM trips_h3
GROUP BY dropoff_cell;

-- Roll up to coarser resolution
SELECT
  h3_toparent(pickup_cell, 7) AS parent_cell,
  count(*) AS trip_count
FROM trips_h3
GROUP BY h3_toparent(pickup_cell, 7);

-- Compact a set of cells for efficient storage
SELECT h3_compact(collect_set(cell)) AS compacted
FROM zipcode_h3
WHERE zipcode = '10001';
```





`GEOMETRY``GEOGRAPHY`





`ST_GeomFromText(wkt [, srid])``GEOMETRY`
`ST_GeomFromWKT(wkt [, srid])``GEOMETRY`
`ST_GeomFromWKB(wkb [, srid])``GEOMETRY`
`ST_GeomFromEWKT(ewkt)``GEOMETRY`
`ST_GeomFromEWKB(ewkb)``GEOMETRY`
`ST_GeomFromGeoJSON(geojson)``GEOMETRY`
`ST_GeomFromGeoHash(geohash)``GEOMETRY`
`ST_GeogFromText(wkt)``GEOGRAPHY`
`ST_GeogFromWKT(wkt)``GEOGRAPHY`
`ST_GeogFromWKB(wkb)``GEOGRAPHY`
`ST_GeogFromEWKT(ewkt)``GEOGRAPHY`
`ST_GeogFromGeoJSON(geojson)``GEOGRAPHY`
`ST_Point(x, y [, srid])``GEOMETRY`
`ST_PointFromGeoHash(geohash)``GEOMETRY`
`to_geometry(georepExpr)``GEOMETRY`
`to_geography(georepExpr)``GEOGRAPHY`
`try_to_geometry(georepExpr)``GEOMETRY`
`try_to_geography(georepExpr)``GEOGRAPHY`





`ST_AsText(geo)``STRING`
`ST_AsWKT(geo)``STRING`
`ST_AsBinary(geo)``BINARY`
`ST_AsWKB(geo)``BINARY`
`ST_AsEWKT(geo)``STRING`
`ST_AsEWKB(geo)``BINARY`
`ST_AsGeoJSON(geo)``STRING`
`ST_GeoHash(geo)``STRING`





`ST_Point(x, y [, srid])`
`ST_MakeLine(pointArray)`
`ST_MakePolygon(outer [, innerArray])`





`ST_X(geo)``DOUBLE`
`ST_Y(geo)``DOUBLE`
`ST_Z(geo)``DOUBLE`
`ST_M(geo)``DOUBLE`
`ST_XMin(geo)``DOUBLE`
`ST_XMax(geo)``DOUBLE`
`ST_YMin(geo)``DOUBLE`
`ST_YMax(geo)``DOUBLE`
`ST_ZMin(geo)``DOUBLE`
`ST_ZMax(geo)``DOUBLE`
`ST_Dimension(geo)``INT`
`ST_NDims(geo)``INT`
`ST_NPoints(geo)``INT`
`ST_NumGeometries(geo)``INT`
`ST_NumInteriorRings(geo)``INT`
`ST_GeometryType(geo)``STRING`
`ST_GeometryN(geo, n)``GEOMETRY`
`ST_PointN(geo, n)``GEOMETRY`
`ST_StartPoint(geo)``GEOMETRY`
`ST_EndPoint(geo)``GEOMETRY`
`ST_ExteriorRing(geo)``GEOMETRY`
`ST_InteriorRingN(geo, n)``GEOMETRY`
`ST_Envelope(geo)``GEOMETRY`
`ST_Envelope_Agg(geo)``GEOMETRY`
`ST_Dump(geo)``ARRAY`
`ST_IsEmpty(geo)``BOOLEAN`





`ST_Area(geo)`
`ST_Length(geo)`
`ST_Perimeter(geo)`
`ST_Distance(geo1, geo2)`
`ST_DistanceSphere(geo1, geo2)`
`ST_DistanceSpheroid(geo1, geo2)`
`ST_Azimuth(geo1, geo2)`
`ST_ClosestPoint(geo1, geo2)`





`ST_Contains(geo1, geo2)`
`ST_Within(geo1, geo2)`
`ST_Intersects(geo1, geo2)`
`ST_Disjoint(geo1, geo2)`
`ST_Touches(geo1, geo2)`
`ST_Covers(geo1, geo2)`
`ST_Equals(geo1, geo2)`
`ST_DWithin(geo1, geo2, distance)`





`ST_Intersection(geo1, geo2)`
`ST_Union(geo1, geo2)`
`ST_Union_Agg(geo)`
`ST_Difference(geo1, geo2)`





`ST_Buffer(geo, radius)`
`ST_Centroid(geo)`
`ST_ConvexHull(geo)`
`ST_ConcaveHull(geo, ratio [, allowHoles])`
`ST_Boundary(geo)`
`ST_Simplify(geo, tolerance)`





`ST_AddPoint(linestring, point [, index])`
`ST_RemovePoint(linestring, index)`
`ST_SetPoint(linestring, index, point)`
`ST_FlipCoordinates(geo)`
`ST_Multi(geo)`
`ST_Reverse(geo)`





`ST_Translate(geo, xOffset, yOffset [, zOffset])`
`ST_Scale(geo, xFactor, yFactor [, zFactor])`
`ST_Rotate(geo, angle)`





`ST_SRID(geo)`
`ST_SetSRID(geo, srid)`
`ST_Transform(geo, targetSrid)`





`ST_IsValid(geo)`



`GEOMETRY``GEOGRAPHY``CREATE TABLE``STRING``ST_GeomFromText()``ST_GeogFromText()`

```sql
-- Create a table with geometry columns (requires serverless DBR 17.1+)
CREATE TABLE retail_stores (
  store_id INT,
  name STRING,
  location GEOMETRY
);

INSERT INTO retail_stores VALUES
  (1, 'Downtown Store', ST_Point(-73.9857, 40.7484, 4326)),
  (2, 'Midtown Store',  ST_Point(-73.9787, 40.7614, 4326)),
  (3, 'Uptown Store',   ST_Point(-73.9680, 40.7831, 4326));

-- Create delivery zones as polygons
CREATE TABLE delivery_zones (
  zone_id INT,
  zone_name STRING,
  boundary GEOMETRY
);

INSERT INTO delivery_zones VALUES
  (1, 'Zone A', ST_GeomFromText(
    'POLYGON((-74.00 40.74, -73.97 40.74, -73.97 40.76, -74.00 40.76, -74.00 40.74))', 4326
  ));

-- Point-in-polygon: find stores within a delivery zone
SELECT s.name, z.zone_name
FROM retail_stores s
JOIN delivery_zones z
  ON ST_Contains(z.boundary, s.location);

-- Distance calculation: find customers within 5km of a store
-- Note: to_geography() expects STRING (WKT/GeoJSON) or BINARY (WKB) input, not GEOMETRY.
-- Use ST_AsText() to convert GEOMETRY to WKT first.
SELECT c.customer_id, c.name,
  ST_DistanceSphere(c.location, s.location) AS distance_meters
FROM customers c
CROSS JOIN retail_stores s
WHERE s.store_id = 1
  AND ST_DWithin(
    ST_GeogFromText(ST_AsText(c.location)),
    ST_GeogFromText(ST_AsText(s.location)),
    5000  -- 5km in meters
  );

-- Buffer zone: create 1km buffer around a store (use projected CRS for meters)
SELECT ST_Buffer(
  ST_Transform(location, 5070),  -- project to NAD83/Albers (meters)
  1000                            -- 1000 meters
) AS buffer_zone
FROM retail_stores
WHERE store_id = 1;

-- Area calculation
SELECT zone_name,
  ST_Area(ST_Transform(boundary, 5070)) AS area_sq_meters
FROM delivery_zones;

-- Union of overlapping zones
SELECT ST_Union_Agg(boundary) AS combined_coverage
FROM delivery_zones;

-- Convert between formats
SELECT
  ST_AsText(location) AS wkt,
  ST_AsGeoJSON(location) AS geojson,
  ST_GeoHash(location) AS geohash
FROM retail_stores;

-- Spatial join with BROADCAST hint for performance
SELECT /*+ BROADCAST(zones) */
  c.customer_id, z.zone_name
FROM customers c
JOIN delivery_zones zones
  ON ST_Contains(zones.boundary, c.location);
```



```sql
-- Use H3 for fast pre-filtering, then ST for precise spatial operations
-- Step 1: Index store locations with H3
CREATE TABLE store_h3 AS
SELECT store_id, name, location,
  h3_longlatash3(ST_X(location), ST_Y(location), 9) AS h3_cell
FROM retail_stores;

-- Step 2: Index customer locations with H3
CREATE TABLE customer_h3 AS
SELECT customer_id, name, location,
  h3_longlatash3(ST_X(location), ST_Y(location), 9) AS h3_cell
FROM customers;

-- Step 3: Fast proximity using H3 pre-filter + precise ST distance
SELECT s.name AS store, c.name AS customer,
  ST_DistanceSphere(s.location, c.location) AS distance_m
FROM store_h3 s
JOIN customer_h3 c
  ON c.h3_cell IN (SELECT explode(h3_kring(s.h3_cell, 2)))
WHERE ST_DistanceSphere(s.location, c.location) < 2000;
```



`ST_Intersects``ST_Contains``ST_Within`


`BROADCAST`



`delta.autoOptimize.optimizeWrite``delta.autoOptimize.autoCompact`











`UTF8_BINARY``'A' < 'Z' < 'a'`
`UTF8_LCASE``'A' == 'a'``'e' != 'e'`
`UNICODE``'a' < 'A' < 'A' < 'b'`
`DE``FR``JA`



```
{ UTF8_BINARY | UTF8_LCASE | { UNICODE | locale } [ _ modifier [...] ] }
```

`locale`
```
language_code [ _ script_code ] [ _ country_code ]
```

`language_code``EN``DE``FR``JA``ZH`
`script_code``Hant``Latn`
`country_code``US``DE``CAN`





`CS``'A' != 'a'`
`CI``'A' == 'a'`
`AS``'e' != 'e'`
`AI``'e' == 'e'`
`RTRIM``'Hello' == 'Hello '`







`UNICODE`
`UNICODE_CI`
`UNICODE_CI_AI`
`DE`
`DE_CI_AI`
`FR_CAN`
`EN_US`
`ZH_Hant_MAC`
`SR``SR_CYR_SRN_CS_AS`
`JA`
`EN_CS_AI`
`UTF8_LCASE_RTRIM`





`COLLATE`










```sql
-- Create catalog with default collation
CREATE CATALOG customer_cat
  DEFAULT COLLATION UNICODE_CI_AI;

-- All schemas, tables, and string columns created in this catalog
-- inherit UNICODE_CI_AI unless overridden
```



```sql
-- Create schema with default collation
CREATE SCHEMA my_schema
  DEFAULT COLLATION UNICODE_CI;

-- Change default collation for new objects (existing objects unchanged)
ALTER SCHEMA my_schema
  DEFAULT COLLATION UNICODE_CI_AI;
```



```sql
-- Table-level default collation
CREATE TABLE users (
  id INT,
  username STRING,           -- inherits UNICODE_CI from table default
  email STRING,              -- inherits UNICODE_CI from table default
  password_hash STRING COLLATE UTF8_BINARY  -- explicit override
) DEFAULT COLLATION UNICODE_CI;
```



```sql
-- Column-level collation
CREATE TABLE products (
  id INT,
  name STRING COLLATE UNICODE_CI,
  sku STRING COLLATE UTF8_BINARY,
  description STRING COLLATE UNICODE_CI_AI
);

-- Add column with collation
ALTER TABLE products
  ADD COLUMN category STRING COLLATE UNICODE_CI;

-- Change column collation (requires DBR 17.2+; may not be available on all SQL Warehouse versions)
ALTER TABLE products
  ALTER COLUMN name SET COLLATION UNICODE_CI_AI;
```



```sql
-- Apply collation inline in a query
SELECT *
FROM products
WHERE name COLLATE UNICODE_CI = 'laptop';

-- Check the collation of an expression
SELECT collation('test' COLLATE UNICODE_CI);
-- Returns: UNICODE_CI
```



```
Catalog DEFAULT COLLATION
  -> Schema DEFAULT COLLATION (overrides catalog)
    -> Table DEFAULT COLLATION (overrides schema)
      -> Column COLLATE (overrides table)
        -> Expression COLLATE (overrides column)
```

`UTF8_BINARY`







`=``!=``<``>``<=``>=`
`LIKE`
`CONTAINS(str, substr)`
`STARTSWITH(str, prefix)`
`ENDSWITH(str, suffix)`
`IN (...)`
`BETWEEN`
`ORDER BY`
`GROUP BY`
`DISTINCT`
`REPLACE(str, old, new)`
`TRIM``LTRIM``RTRIM`

`STARTSWITH``ENDSWITH``UTF8_LCASE``LOWER()`



```sql
-- Get collation of an expression
SELECT collation(name) FROM products;

-- List all supported collations
SELECT * FROM collations();

-- Test collation with COLLATE
SELECT collation('hello' COLLATE DE_CI_AI);
-- Returns: DE_CI_AI
```





```sql
-- Using column collation (preferred - leverages indexes)
CREATE TABLE users (
  id INT,
  username STRING COLLATE UTF8_LCASE,
  email STRING COLLATE UTF8_LCASE
);

INSERT INTO users VALUES
  (1, 'JohnDoe', 'John@Example.com'),
  (2, 'janedoe', 'JANE@EXAMPLE.COM');

-- Case-insensitive match automatically
SELECT * FROM users WHERE username = 'johndoe';
-- Returns: JohnDoe

SELECT * FROM users WHERE email = 'john@example.com';
-- Returns: John@Example.com
```



```sql
-- Ad-hoc case-insensitive comparison on a UTF8_BINARY column
SELECT * FROM products
WHERE name COLLATE UNICODE_CI = 'MacBook Pro';
-- Matches: macbook pro, MACBOOK PRO, MacBook Pro, etc.
```



```sql
-- Accent-insensitive matching
CREATE TABLE cities (
  id INT,
  name STRING COLLATE UNICODE_CI_AI
);

INSERT INTO cities VALUES (1, 'Montreal'), (2, 'Montreal');

SELECT * FROM cities WHERE name = 'Montreal';
-- Returns both: Montreal and Montreal (treats e and e as equal)
```



```sql
-- German sorting (umlauts sort correctly)
SELECT name
FROM german_customers
ORDER BY name COLLATE DE;
-- Sorts: Arzte before Bauer (A treated as A+e in German sorting)

-- Swedish sorting (A, A, O sort after Z)
SELECT name
FROM swedish_customers
ORDER BY name COLLATE SV;
```



```sql
-- RTRIM modifier ignores trailing spaces
SELECT 'Hello' COLLATE UTF8_BINARY_RTRIM = 'Hello   ';
-- Returns: true

SELECT 'Hello' COLLATE UTF8_BINARY = 'Hello   ';
-- Returns: false
```



```sql
-- Create a catalog where everything is case-insensitive by default
CREATE CATALOG app_data DEFAULT COLLATION UNICODE_CI;

USE CATALOG app_data;
CREATE SCHEMA users_schema;
USE SCHEMA users_schema;

-- All STRING columns automatically use UNICODE_CI
CREATE TABLE accounts (
  id INT,
  username STRING,  -- UNICODE_CI inherited from catalog
  email STRING       -- UNICODE_CI inherited from catalog
);

-- Queries are automatically case-insensitive
SELECT * FROM accounts WHERE username = 'admin';
-- Matches: Admin, ADMIN, admin, aDmIn, etc.
```



`CHECK``UTF8_BINARY`
`hive_metastore`
`ALTER SCHEMA ... DEFAULT COLLATION`

`UTF8_LCASE`
`SR_CYR_SRN_CS_AS``SR`

`DEFAULT COLLATION`
