---
title: Unstructured PDF Generation
description: Generate synthetic PDF documents for RAG and unstructured data use cases. Use when creating test PDFs, demo documents, or evaluation datasets for retrieval s...
sidebar:
  label: Unstructured PDF Generation
  order: 0
  badge:
    text: New
    variant: tip
---







`generate_and_upload_pdfs`
`generate_and_upload_pdf`










`generate_and_upload_pdfs`
`catalog`
`schema`
`description`
`count`

`/Volumes/my_catalog/my_schema/raw_data/pdf_documents/`



`generate_and_upload_pdf`
`title`
`description`
`question`
`guideline`
`catalog`
`schema`











`catalog`
`schema`
`description`
`count`
`volume``raw_data`
`folder``pdf_documents`
`doc_size``MEDIUM``SMALL``MEDIUM``LARGE`
`overwrite_folder``false`









`title`
`description`
`question`
`guideline`
`catalog`
`schema`
`volume``raw_data`
`folder``pdf_documents`
`doc_size``MEDIUM``SMALL``MEDIUM``LARGE`











`<model_id>.pdf`
`<model_id>.json`



```json
{
  "title": "API Authentication Guide",
  "category": "Technical",
  "pdf_path": "/Volumes/catalog/schema/volume/folder/doc_001.pdf",
  "question": "What authentication methods are supported by the API?",
  "guideline": "Answer should mention OAuth 2.0, API keys, and JWT tokens with their use cases."
}
```





`generate_and_upload_pdfs`
`catalog`
`schema`
`description`
`count`
`folder`
`overwrite_folder`



`generate_and_upload_pdfs`
`catalog`
`schema`
`description`
`count`
`folder`
`overwrite_folder`



`generate_and_upload_pdfs`
`catalog`
`schema`
`description`
`count`
`folder`
`overwrite_folder`



`generate_and_upload_pdfs`
`catalog`
`schema`
`description`
`count`
`folder`
`overwrite_folder`



`ai_dev_kit`

`generate_and_upload_pdfs`














`hr_policies/`
`technical_docs/`
`training_materials/`

`true`






`question`
`guideline`


```python
# Load questions from JSON files
questions = load_json_files(f"/Volumes/{catalog}/{schema}/{volume}/{folder}/*.json")

for q in questions:
    # Query RAG system
    response = rag_system.query(q["question"])

    # Evaluate using guideline
    is_correct = evaluate_response(response, q["guideline"])
```



`databricks-gpt-*`

```bash
# Optional: Override auto-discovery
DATABRICKS_MODEL=databricks-gpt-5-4           # Main model for content generation
DATABRICKS_MODEL_NANO=databricks-gpt-5-4-nano # Smaller model (used by default for speed)
```

`databricks-gpt-*`





`databricks-gpt-*``DATABRICKS_MODEL`

`count``doc_size: "SMALL"`
`description`







